import { TerminalClient } from "@/components/TerminalClient";

export default function TerminalPage() {
  return (
    <TerminalClient
      title="Terminal"
      blurb="Every coin is scored 0–100 before it can touch the book. Narrower strategies. Cheaper effective fee. Paper fills on live Solana events."
    />
  );
}
