import { TerminalClient } from "@/components/TerminalClient";

export default function MigratePage() {
  return (
    <TerminalClient
      title="Migration sniper"
      blurb="Graduation is the rare honest event on Pump.fun and LaunchLab. We watch 82%+ bonding with organic unique flow — not the last bundled tick."
      strategy="migration_snipe"
    />
  );
}
