import { TerminalClient } from "@/components/TerminalClient";

export default function CopyPage() {
  return (
    <TerminalClient
      title="Copy"
      blurb="Follow flow that looks like smart money — high unique traders, organic buys, deployer that is not a graveyard. We do not copy every wallet that printed once."
      strategy="copy_trade"
    />
  );
}
